import { FilterEnvioPipe } from './filter-envio.pipe';

describe('FilterEnvioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEnvioPipe();
    expect(pipe).toBeTruthy();
  });
});
