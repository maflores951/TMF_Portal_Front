import { FilterParametroPipe } from './filter-parametro.pipe';

describe('FilterParametroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParametroPipe();
    expect(pipe).toBeTruthy();
  });
});
