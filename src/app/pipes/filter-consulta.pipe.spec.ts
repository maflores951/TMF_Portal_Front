import { FilterConsultaPipe } from './filter-consulta.pipe';

describe('FilterConsultaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConsultaPipe();
    expect(pipe).toBeTruthy();
  });
});
