import { FilterRolPipe } from './filter-rol.pipe';

describe('FilterRolPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRolPipe();
    expect(pipe).toBeTruthy();
  });
});
