import { FilterSuaPipe } from './filter-sua.pipe';

describe('FilterSuaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSuaPipe();
    expect(pipe).toBeTruthy();
  });
});
